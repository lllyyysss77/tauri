interface:
  display_name: "Tauri"
  short_description: "Repo-specific patterns and workflows for tauri"
  default_prompt: "Use the tauri repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true